#!/bin/bash

# DEPRECATED: Legacy wrapper for fix-all-validation-issues.sh
# This script is maintained for backward compatibility.
# New usage: ./scripts/dds-scripts.sh character fix-validation
# Direct usage: ./scripts/character-management/fix-validation-issues.sh

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Forward all arguments to the new refactored script
exec "$SCRIPT_DIR/character-management/fix-validation-issues.sh" "$@"
